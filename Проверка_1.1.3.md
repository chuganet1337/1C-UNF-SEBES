# Проверка расширения «Аудит и восстановление себестоимости» 1.1.3

Дата: 15.09.2026

Цель выпуска: устранить ошибку инициализации модуля из-за недоступной в целевой платформе функции `Абс()`.

## Исправление

- Добавлена серверная функция `АбсолютноеЗначение()` без зависимости от платформенной функции `Абс()`.
- Заменены все семь вызовов `Абс()` в проверках остатков, движений запасов и себестоимости продаж.
- Тест выпуска проверяет наличие новой функции и отсутствие строки `Абс(` в BSL-модуле после обратной распаковки CFE.

## Выполнено автоматически

- CFE собран локальным `v8unpack 1.2.11` и успешно распакован обратно.
- BSL-модуль после обратной распаковки совпадает с исходником.
- BSL Language Server: 0 ошибок, 5 предупреждений сложности/стиля.
- Структурный тест: 43 из 43 проверки успешно.
- Размер CFE: 20 682 байта.
- SHA-256: `1D2AEC7E47D45D299AB89327984C7308EADF74FEE0A9AB60C321CDBE8189054D`.

## Что необходимо проверить в 1С

- заменить расширение версией 1.1.3;
- выполнить `Проверку возможности применения всех расширений`;
- открыть рабочее место и убедиться, что модуль инициализируется без ошибки `Абс`;
- выполнить проверку структуры и аудит на копии базы.

На этой машине платформа 1С отсутствует, поэтому применение CFE и выполнение формы в реальном сеансе не проверялись.
