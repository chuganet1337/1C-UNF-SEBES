# Проверка расширения «Аудит и восстановление себестоимости» 1.1.2

Дата: 15.09.2026

Цель выпуска: уникализировать имена всех собственных метаданных и устранить ошибку создания формы `Переменная не определена (Команды)`.

## Исправления

- Расширение: `ИСС_АудитСебестоимости`.
- Обработка и подсистема: `ИСС_АудитСебестоимости`.
- Основная роль: `ИСС_ОсновнаяРольАудитаСебестоимости`.
- Язык: `ИСС_Русский`.
- Форма: `ИСС_ФормаАудитаСебестоимости`.
- Префикс расширения: `ИСС_`; префикс донора `Расш1_` удален из актуального контейнера.
- Процедура добавления команд переведена с `&НаСервереБезКонтекста` на `&НаСервере` и использует `ЭтаФорма.Команды.Добавить()`.

## Выполнено автоматически

- CFE собран локальным `v8unpack 1.2.11` и успешно распакован обратно.
- BSL-модуль после обратной распаковки совпадает с исходником.
- BSL Language Server: 0 ошибок, 5 предупреждений сложности/стиля.
- Структурный тест: 41 из 41 проверки успешно.
- Размер CFE: 20 645 байт.
- SHA-256: `40D2F26B5DD73EE8ACDA7A81B7039E1D5D72B525D632BD3FB512689CE83B08F5`.

## Что необходимо проверить в 1С

- загрузить 1.1.2 как новое расширение `ИСС_АудитСебестоимости`;
- выполнить `Проверку возможности применения всех расширений`;
- открыть рабочее место и убедиться, что форма создается без ошибки `Команды`;
- выполнить проверку структуры и аудит на копии базы;
- перепроведение проверять только после резервной копии.

На этой машине платформа 1С отсутствует, поэтому применение CFE и выполнение формы в реальном сеансе не проверялись.
